# Frontend (React)

Install & run dev:
- `cd frontend && npm install && npm start`

Or build & serve (Dockerfile included)

Default API URL: http://localhost:8080 (change with REACT_APP_API_URL env var)
